export enum RequestStateEnum {
  PENDING,
  ACTIVE,
  FINISHED,
  DECLINED,
  ABORTED,
  COMPLETED,
  PAID
}

export enum UserConsentStateEnum {
  ACCPETED,
  REFUSED,
  IS_PENDING
}

export enum QueryTypesEnum {
  PUBLIC,
  PIN
}
