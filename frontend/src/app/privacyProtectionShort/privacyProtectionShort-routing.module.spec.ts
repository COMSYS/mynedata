import { PrivacyProtectionShortRoutingModule } from './privacyProtectionShort-routing.module';

describe('PrivacyProtectionShortRoutingModule', () => {
  let PrivacyProtectionShortRoutingModule: PrivacyProtectionShortRoutingModule;

  beforeEach(() => {
    PrivacyProtectionShortRoutingModule = new PrivacyProtectionShortRoutingModule();
  });

  it('should create an instance', () => {
    expect(PrivacyProtectionShortRoutingModule).toBeTruthy();
  });
});
