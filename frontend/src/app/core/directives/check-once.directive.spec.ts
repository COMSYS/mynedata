import { CheckOnceDirective } from './check-once.directive';

describe('CheckOnceDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckOnceDirective();
    expect(directive).toBeTruthy();
  });
});
